import pdkSdkValidator from './pdkSdkValidator'

export default {
  validators: [
    pdkSdkValidator
    // configValidator,
    // packageValidator
    // recommandValidator,
    // eslintValidator
  ]
}
