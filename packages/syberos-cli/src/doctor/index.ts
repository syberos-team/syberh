import pdkSdkValidator from './pdkSdkValidator'

export default {
  validators: [
    // configValidator,
    // packageValidator,
    // recommandValidator,
    // eslintValidator
    pdkSdkValidator
  ]
}
